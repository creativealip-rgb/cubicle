import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";const all=readFileSync("src/lib/actions/invoices.ts","utf8"),s=all.slice(all.indexOf("export async function deleteInvoice(invoiceId"));describe("retainer invoice delete",()=>{it("reopens its invoiced period before deletion",()=>{expect(s).toContain("if (inv.retainerPeriodId)");expect(s).toContain('status: "locked"');expect(s).toContain("invoiceGeneration: sql");expect(s.indexOf("if (inv.retainerPeriodId)")).toBeLessThan(s.indexOf("await tx.delete(invoiceItems)"));});});