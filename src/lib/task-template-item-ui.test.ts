import{describe,expect,it}from"vitest";import fs from"node:fs";const s=fs.readFileSync("src/components/tasks/task-template-workspace.tsx","utf8");describe("template item UI",()=>{it("edits all fields",()=>{expect(s).toContain("ItemFormDialog");expect(s).toContain("defaultAssigneeId");expect(s).toContain("updateTaskTemplateItem")});it("moves exactly one step with boundaries",()=>{expect(s).toContain("moveItem");expect(s).toContain('direction:"up"|"down"');expect(s).toContain("index===0");expect(s).toContain("index===all.length-1")});it("blocks archived writes",()=>expect(s).toContain('template.status === "active"'))});