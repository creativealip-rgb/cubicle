import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";const read=(p:string)=>readFileSync(p,"utf8");describe("source invoice amount display",()=>{it("uses persisted source amount instead of rounded quantity",()=>{const editor=read("src/components/invoices/invoice-full-editor.tsx"),page=read("src/app/(app)/app/invoices/[invoiceId]/page.tsx");expect(editor).toContain("item.amount ??");expect(page).toContain("amount: Number(item.amount)");});});