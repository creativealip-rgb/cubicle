import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";const r=(p:string)=>readFileSync(p,"utf8");describe("billing terminal UX",()=>{it("disables zero-total invoice submit",()=>expect(r("src/components/forms/invoice-form.tsx")).toContain("invoiceTotal <= 0"));it("does not claim an invoiced retainer was created",()=>expect(r("src/components/invoices/retainer-project-invoice-actions.tsx")).toContain('current.status === "invoiced"'));});