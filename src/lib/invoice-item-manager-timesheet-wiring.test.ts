import {describe,expect,it} from "vitest";import{readFileSync}from"node:fs";const read=(p:string)=>readFileSync(p,"utf8");describe("invoice item manager timesheet source",()=>{it("queries eligible approved billable time",()=>{const s=read("src/app/(app)/app/invoices/[invoiceId]/page.tsx");expect(s).toContain("eligibleTimeEntries");expect(s).toContain('eq(timeEntries.status, "approved")');expect(s).toContain("timeEntries={eligibleTimeEntries}");});it("offers timesheet in single item manager",()=>{const s=read("src/app/(app)/app/invoices/[invoiceId]/add-item-button.tsx");expect(s).toContain('"timesheet"');expect(s).toContain("ImportTimeSection");expect(s).toContain("Time Entries (Timesheet)");});});